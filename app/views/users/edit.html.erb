<h1>Edit User Profile</h1>

<%= form_for(@user) do |f| %>
  <%= f.file_field :avatar %>
  <%= f.label :username %>
  <%= f.text_field :username %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.submit "Save changes"%>
<% end %>